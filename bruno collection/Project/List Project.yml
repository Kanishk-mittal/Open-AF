info:
  name: List Project
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/api/v1/project"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
