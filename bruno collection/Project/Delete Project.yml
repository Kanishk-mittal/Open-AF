info:
  name: Delete Project
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseURL}}/api/v1/projects/:projectId"
  params:
    - name: projectId
      value: e2f59ebc-af1b-424a-abd4-ebeed4f827db
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
