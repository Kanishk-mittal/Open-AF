info:
  name: List Devices
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/v1/projects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
